def reverse_with_frozen_indices(image, frozen_indices):
    """
    Reverses a list of integers in-place while keeping the elements at frozen indices unchanged.
    
    Parameters:
    image (list): The list of integers representing the image.
    frozen_indices (list): A list of integers representing the indices that should remain in place.
    
    Returns:
    None: The function modifies the list in-place and does not return a value.
    """
    # Placeholder for implementation
    pass
