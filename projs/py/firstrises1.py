def first_rises(prices):
    """
    Computes the first rise in stock price for each price point.

    Args:
    prices: List of stock prices at each time point.

    Returns:
    List[int]: A list where each element represents the first rise in value after each price point.
               If no rise exists for a price point, the value is -1.
    """
    
    # Placeholder return for now
    return [-1] * len(prices)
