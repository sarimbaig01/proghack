// heap: the heap vector, i: index to start heapifying down
void heapifyDown(std::vector<int>& heap, int i);

// heap: the heap vector, i: index to start heapifying up
void heapifyUp(std::vector<int>& heap, int i);
