// Get the position and size of the paddle
sf::Vector2f paddlePosition = paddle.getPosition(); // (x, y) coordinates of the top-left corner of the paddle
float paddleWidth = paddle.getSize().x; // Width of the paddle
float paddleHeight = paddle.getSize().y; // Height of the paddle
