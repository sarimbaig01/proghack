#include <vector>

int maximum_rise(const std::vector<int>& prices) {
    /**
     * Computes the maximum rise in stock price between two time points.
     *
     * Args:
     * prices (vector of int): List of stock prices at each time point.
     *
     * Returns:
     * int: The maximum rise (prices[j] - prices[i]).
     */

    return -1;
}
